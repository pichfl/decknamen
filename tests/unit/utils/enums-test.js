import enums from 'game/utils/enums';
import { module, test } from 'qunit';

module('Unit | Utility | enums', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = enums();
    assert.ok(result);
  });
});
