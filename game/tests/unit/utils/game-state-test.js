import GameState from 'game/utils/game-state';
import { module, test } from 'qunit';

module('Unit | Utility | game-state', function () {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = new GameState();
    assert.ok(result);
  });
});
