import getServer from 'game/utils/get-server';
import { module, test } from 'qunit';

module('Unit | Utility | get-server', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = getServer();
    assert.ok(result);
  });
});
