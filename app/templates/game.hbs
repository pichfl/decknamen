<div class="flex flex-col min-h-screen p-5 justify-center">
  <Game::Table class="mt-auto" @cards={{this.model}} @onChange={{this.onChange}} />
  <Game::Tally @cards={{this.model}} class="mt-16 mb-auto">
    <button type="button" class="button">
      Skip Turn
    </button>

    <button type="button" class="button ml-2">
      End Game
    </button>
  </Game::Tally>
</div>

{{outlet}}